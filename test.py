import json
with open("./test.txt", 'w') as f:
    # value = ('the answer', 42)
    # s = str(value)  # convert the tuple to string
    print(f.write(s))

    x = [1, 'simple', 'list']
    json.dumps(x)
    json.dump(x, f)


